// Cria uma função que recebe um array e um número como argumentos e verifica se possui algum elemento menor que n.

// Utiliza o que aprendeste sobre o some para resolver este exercício.
function verificaElemento(array, n) {
  return array.some((elem) => elem < n);
}
