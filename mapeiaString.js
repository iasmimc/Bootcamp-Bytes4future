// cria uma funcao que mapeia cada elemento de um array de strings para o seu comprimento.

function mapeiaStrings(array) {
  return array.map((elemento) => elemento.length);
}
console.log(["ola", "mundo", "tudo", "bem?"]);
