// Cria uma função que recebe uma data e um número de horas
// como argumentos e retorna uma nova data resultante da subtração.

function subtraiHoras(data, horas) {
  // escreve aqui a função
  data.setHours(data.getHours() - horas);
  return data;
}
