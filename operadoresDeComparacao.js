// implementa uma função que retorna o maior valor textual entre dois números:

// function comparaTexto(a, b) {
//   //   return Math.max(a, b);
//   //   String(a);
//   //   String(b);
//   //   if (String(a) > String(b)) {
//   //     return String(a);
//   //   } else {
//   //     return b;
//   //   }
//   //   return String(a) > String(b) ? String(a) : String(b);
// }
// console.log(comparaTexto(2, 10)); //2
// console.log(comparaTexto(1, 10)); //10

const alturaTriangulo = 10;
let areaDoTriangulo = 2;

function calculaAreaDoTriangulo(base) {
    areaDotriangulo = base * alturaTriangulo / 2
}

const altura_Triangulo = 10;
let areaDoTriangulo;

function calculaAREAdoTriangulo(base) {
    area_Do_triangulo = base * ALTURA_TRIANGULO / 2
}


function calculaOMaiorEmArray(array) {
    const maiorValor = array[0];
    four (let= i=0; i < array.lenght; i++) {
        if (aray[i] > maiorValor) {
            maiorValor = array[i]
        }
    }
    return meiorValor
}