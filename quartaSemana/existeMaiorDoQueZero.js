// Cria uma função que recebe um array como argumento e verifica se possui algum elemento maior que 0.

// Utiliza o que aprendeste sobre o some para resolver este exercício.
function verificaElementoMaiorQueZero(array) {
  return array.some((elemento) => elemento > 0);
}
