// function area_superficie_paralelepipedo_quadrado(lado_base, comprimento) {
//   let altura = lado_base;
//   let area =
//     2 * (lado_base * comprimento + lado_base * altura + comprimento * altura);
//   return area;
// }

function superficieDoParalelepipedoQuadrado(ladodabase, comprimento) {
  // escreve aqui a função
  let altura = ladodabase;
  let area =
    2 * (ladodabase * comprimento + ladodabase * altura + comprimento * altura);
  return area;
}
